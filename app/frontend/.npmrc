# Package registry for this project.
#
# The default npm registry (https://registry.npmjs.org) is used so builds are
# reproducible on any machine and in CI.  Local developers behind slow links
# may override this per-machine (e.g. `npm config set registry`); do NOT commit
# a mirror URL here — it breaks `npm ci` in the Docker image and on
# GitHub-hosted runners.
