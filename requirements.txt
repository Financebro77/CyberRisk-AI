# Vercel / serverless runtime dependencies for the CyberRisk AI web app.
#
# This file is the dependency manifest for the Vercel Python Function (the
# zero-config FastAPI deployment in `api/vc_app.py`).  It mirrors the
# `[project]` base deps plus the `web` and `agent` extras from pyproject.toml —
# the exact module-level import closure the FastAPI app needs at runtime
# (test, reporting, and streamlit-UI deps are intentionally omitted: they would
# only bloat the serverless bundle).  Local installs still use pyproject.toml:
#   pip install -e ".[web,agent]"
numpy>=1.23
pandas>=1.5
scipy>=1.9
pyyaml>=6.0
pydantic>=2.0
fastapi>=0.110
uvicorn>=0.29
openai>=1.0
python-dotenv>=1.0
requests>=2.28
