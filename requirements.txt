# Runtime dependencies for the CyberRisk AI web app.
#
# Source of truth is pyproject.toml `[project] dependencies` (Vercel's Python
# runtime installs from there in services mode). This file mirrors the same
# set for deploy targets that only read requirements.txt (Docker, Render,
# `pip install -r requirements.txt`). Local installs use pyproject.toml:
#   pip install -e ".[web,agent]"
numpy>=1.23
pandas>=1.5
scipy>=1.9
pyyaml>=6.0
pydantic>=2.0
matplotlib>=3.6
openpyxl>=3.0
xlsxwriter>=3.0
fastapi>=0.110
uvicorn>=0.29
openai>=1.0
python-dotenv>=1.0
requests>=2.28
